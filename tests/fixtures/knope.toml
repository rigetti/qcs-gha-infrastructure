# A file already tracked on the default branch stands in for a changelog:
# the action restores whatever path knope.toml names, and using README.md means
# this test works on the very first pull request, before any fixture of its own
# has landed on the default branch.
[package]
changelog = "README.md"
